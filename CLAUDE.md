<!-- Claude Code loads CLAUDE.md; AGENTS.md is the single source of truth, imported below.
     A file import (not a symlink) so it works on Windows and with core.symlinks=false checkouts. -->
@AGENTS.md
